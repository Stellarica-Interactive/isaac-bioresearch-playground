# Generated by tools/import_mod_channel.py from slo1iso.mod.
# Nicoletti M, Chiodo L, Loppini A, Liu Q, Folli V, Ruocco G, Filippi S.
# Biophysical modeling of the whole-cell dynamics of C. elegans motor and
# interneurons families. PLoS ONE 19(3):e0298105 (2024).
#
# Line numbers refer to the published .mod file, so every constant can be
# checked against the source. Regenerate rather than editing.

[channel]
name = "slo1iso"
source = "slo1iso.mod"
ion = "k"
states = ["m", "FROM", "TO"]
current = "gbar * m * (v - ek)"

[parameters]
c1 = 1  # line 40, (1)
eca = 60  # line 28, (mV)
ek = -80  # line 27, (mV)
fondo = 0.05  # line 26, (uM)
gbar = 0.11  # line 31, (S/cm2)
kxy = 55.726186  # line 38, (/ms)
kyx = 34.338784  # line 34, (uM)
nxy = 1.299198  # line 39, (1)
nyx = 0.0001  # line 35, (1)
wom = 3.152961  # line 32, (/ms)
wop = 0.156217  # line 36, (/ms)
wxy = -0.027527  # line 37, (/mV)
wyx = 0.012643  # line 33, (/mV)

[formulas]

# From a PROCEDURE body. `order` is the sequence the source evaluates
# in, and it is load-bearing: later expressions use names bound by
# earlier ones, so evaluating these in any other order is wrong.
[procedure]
order = ["s0", "v0", "minf", "mtau"]

[procedure.expressions]
s0 = "1/(wyx-wxy)"
v0 = "s0*(log(wom/wop)+log(1+pow(kxy/(ca*1e3),nxy))-log(1+pow((ca*1e3)/kyx,nyx)))"
minf = "1/(1+exp(-(v-v0)/s0))"
mtau = "((exp(wxy*v)/wop)*(1+pow(kxy/(ca*1e3),nxy))*(1/(1+exp(-(v-v0)/s0))))*c1"
