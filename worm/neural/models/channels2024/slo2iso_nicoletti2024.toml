# Generated by tools/import_mod_channel.py from slo2iso.mod.
# Nicoletti M, Chiodo L, Loppini A, Liu Q, Folli V, Ruocco G, Filippi S.
# Biophysical modeling of the whole-cell dynamics of C. elegans motor and
# interneurons families. PLoS ONE 19(3):e0298105 (2024).
#
# Line numbers refer to the published .mod file, so every constant can be
# checked against the source. Regenerate rather than editing.

[channel]
name = "slo2iso"
source = "slo2iso.mod"
ion = "k"
states = ["m", "FROM", "TO"]
current = "gbar * m * (v - ek)"

[parameters]
c2 = 1  # line 39, (1)
eca = 60  # line 26, (mV)
ek = -80  # line 25, (mV)
gbar = 0.1  # line 30, (S/cm2)
kxy1 = 93.449423  # line 37, (/ms)
kyx1 = 3294.553404  # line 33, (uM)
nxy1 = 1.835067  # line 38, (1)
nyx1 = 1e-05  # line 34, (1)
wom1 = 0.896395  # line 31, (/ms)
wop1 = 0.026719  # line 35, (/ms)
wxy1 = -0.024123  # line 36, (/mV)
wyx1 = 0.019405  # line 32, (/mV)

[formulas]

# From a PROCEDURE body. `order` is the sequence the source evaluates
# in, and it is load-bearing: later expressions use names bound by
# earlier ones, so evaluating these in any other order is wrong.
[procedure]
order = ["s0", "v0", "minf", "mtau"]

[procedure.expressions]
s0 = "1/(wyx1-wxy1)"
v0 = "s0*(log(wom1/wop1)+log(1+pow(kxy1/(ca*1e3),nxy1))-log(1+pow((ca*1e3)/kyx1,nyx1)))"
minf = "1/(1+exp(-(v-v0)/s0))"
mtau = "((exp(wxy1*v)/wop1)*(1+pow(kxy1/(ca*1e3),nxy1))*(1/(1+exp(-(v-v0)/s0))))*c2"
